"""TODO: replace this placeholder package with your own code.

Rename the ``src/your_package`` directory and update ``[tool.hatch.build...]``,
``known-first-party``, and ``[tool.coverage.run]`` in ``pyproject.toml`` to match.
"""

from __future__ import annotations

__version__ = "0.1.0"


def add(a: int, b: int) -> int:
    """Return the sum of two integers — sample function, delete me."""
    return a + b
